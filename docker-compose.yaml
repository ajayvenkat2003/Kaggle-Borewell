services:
  mlflow:
    image: ghcr.io/mlflow/mlflow:latest
    ports:
      - 5000:5000
    command: mlflow server --host 0.0.0.0
  application:
    image: app:latest
    ports:
      - 80:80
    network_mode: host
    depends_on:
      - mlflow
    command: uv run train
    
    


